# ADBC transport (default): first-class PostgreSQL driver, libpq COPY-based Arrow ingestion.
adbc-driver-postgresql
adbc-driver-manager
# SQLAlchemy transport: async DBAPI for postgresql+asyncpg.
asyncpg
