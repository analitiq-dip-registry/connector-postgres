# THIS connector's drivers only (the engine pins no database drivers).
# asyncpg drives the SQLAlchemy transport; the ADBC pair drives the
# libpq-based ADBC transport (transport_type: "adbc").
asyncpg>=0.28.0
adbc-driver-postgresql>=1.1.0
adbc-driver-manager>=1.1.0
